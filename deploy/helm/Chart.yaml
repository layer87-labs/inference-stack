apiVersion: v2
name: inference-stack
description: >
  Inference Stack — unified CPU-based model serving for embedding,
  reranking and transcription. Includes TEI (BGE-M3), FlagEmbedding Reranker
  (BGE-reranker-v2-m3), Whisper ASR and an inference-router that exposes
  all models under a single OpenAI-compatible endpoint.

type: application
version: 0.1.0
appVersion: "0.1.0"

maintainers:
  - name: Layer87 Labs
    url: https://github.com/layer87-labs

keywords:
  - inference
  - embeddings
  - reranker
  - whisper
  - tei
  - bge-m3
  - openai-compatible
